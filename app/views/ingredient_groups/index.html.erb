<h1>Listing ingredient_groups</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @ingredient_groups.each do |ingredient_group| %>
      <tr>
        <td><%= ingredient_group.name %></td>
        <td><%= link_to 'Show', ingredient_group %></td>
        <td><%= link_to 'Edit', edit_ingredient_group_path(ingredient_group) %></td>
        <td><%= link_to 'Destroy', ingredient_group, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Ingredient group', new_ingredient_group_path %>
