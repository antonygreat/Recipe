<p id="notice"><%= notice %></p>

<p>
 <strong><%= @ingredient_group.name %></strong>
</p>

<table>
  <% @ingredients.each do |ingredient| %>
  <tr>
    <td><%= link_to ingredient.name, ingredient %></td>
  </tr>
  <% end %>
</table>


<%= link_to 'Back', ingredient_groups_path %>
