<%= form_for(@type_set) do |f| %>
  <% if @type_set.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@type_set.errors.count, "error") %> prohibited this type_set from being saved:</h2>

      <ul>
      <% @type_set.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :recipeid %><br>
    <%= f.number_field :recipeid %>
  </div>
  <div class="field">
    <%= f.label :typeid %><br>
    <%= f.number_field :typeid %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
